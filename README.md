# 🍳 Система Бронирования для Кулинарной Школы

Полнофункциональная система бронирования занятий с интеграцией Stripe для приёма предоплаты.

## 🌟 Возможности

- ✅ **Календарь занятий** - отображение всех доступных мастер-классов
- 👥 **Система capacity** - контроль количества участников на каждом занятии
- 💳 **Stripe интеграция** - безопасная оплата картой
- 📧 **Email уведомления** - автоматические подтверждения бронирования
- 📱 **Адаптивный дизайн** - работает на всех устройствах
- 🔄 **Отмена бронирований** - возврат средств через Stripe
- 📊 **Управление бронированиями** - просмотр всех бронирований

## 🛠️ Технологии

**Backend:**
- Node.js + Express
- Stripe API
- CORS для cross-origin запросов

**Frontend:**
- Vanilla JavaScript (без фреймворков)
- Stripe.js для обработки платежей
- Modern CSS с градиентами и анимациями

## 📋 Требования

- Node.js (версия 14 или выше)
- npm или yarn
- Аккаунт Stripe (можно использовать тестовый режим)

## 🚀 Установка

### 1. Клонирование и установка зависимостей

```bash
# Установите зависимости
npm install
```

### 2. Настройка Stripe

1. Зарегистрируйтесь на https://stripe.com
2. Перейдите в Dashboard → Developers → API keys
3. Скопируйте тестовые ключи:
   - Publishable key (pk_test_...)
   - Secret key (sk_test_...)

### 3. Конфигурация переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и добавьте ваши ключи:

```env
STRIPE_SECRET_KEY=sk_test_ваш_секретный_ключ
STRIPE_PUBLISHABLE_KEY=pk_test_ваш_публичный_ключ
PORT=3000
```

### 4. Обновление ключей в коде

#### В файле `index.html`:
Найдите строку:
```javascript
const STRIPE_PUBLIC_KEY = 'pk_test_51QQcTcP3example_your_key_here';
```
Замените на ваш публичный ключ Stripe.

#### В файле `server.js`:
Найдите строку:
```javascript
const stripe = require('stripe')('sk_test_your_secret_key_here');
```
Замените на ваш секретный ключ Stripe (или используйте переменную окружения).

### 5. Настройка Webhook (для production)

1. В Stripe Dashboard перейдите в Developers → Webhooks
2. Добавьте endpoint: `https://ваш-домен.com/webhook`
3. Выберите события для отслеживания:
   - `payment_intent.succeeded`
   - `payment_intent.payment_failed`
   - `charge.refunded`
4. Скопируйте Webhook signing secret и добавьте в `.env`:

```env
STRIPE_WEBHOOK_SECRET=whsec_ваш_webhook_secret
```

## 🎯 Запуск

### Development режим

```bash
# Запуск сервера
npm start

# Или с автоматической перезагрузкой (требуется nodemon)
npm run dev
```

Сервер запустится на http://localhost:3000

### Открытие в браузере

Откройте `index.html` в браузере. Убедитесь, что он подключается к API на `http://localhost:3000/api`

## 🧪 Тестирование

### Тестовые карты Stripe

Для тестирования используйте следующие данные карт:

**Успешный платёж:**
- Номер карты: `4242 4242 4242 4242`
- Дата: любая будущая дата (например, 12/34)
- CVC: любые 3 цифры
- Почтовый индекс: любой

**Отклонённый платёж:**
- Номер карты: `4000 0000 0000 0002`

**Требуется аутентификация:**
- Номер карты: `4000 0025 0000 3155`

Полный список тестовых карт: https://stripe.com/docs/testing

## 📖 API Endpoints

### GET `/api/classes`
Получить список всех занятий

### GET `/api/classes/:id`
Получить информацию о конкретном занятии

### POST `/api/create-payment-intent`
Создать намерение платежа

**Body:**
```json
{
  "classId": 1,
  "participants": 2,
  "email": "user@example.com",
  "name": "Иван Иванов"
}
```

### POST `/api/confirm-booking`
Подтвердить бронирование после успешной оплаты

**Body:**
```json
{
  "paymentIntentId": "pi_...",
  "classId": 1,
  "name": "Иван Иванов",
  "email": "user@example.com",
  "phone": "+371 12345678",
  "participants": 2
}
```

### POST `/api/cancel-booking`
Отменить бронирование с возвратом средств

**Body:**
```json
{
  "bookingId": 1,
  "email": "user@example.com"
}
```

### GET `/api/bookings`
Получить все бронирования

### GET `/api/bookings/email/:email`
Получить бронирования конкретного пользователя

### POST `/webhook`
Webhook endpoint для событий Stripe

## 🗄️ База данных

В текущей версии данные хранятся в памяти (массивы). Для production рекомендуется:

### PostgreSQL Schema:

```sql
CREATE TABLE classes (
    id SERIAL PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    description TEXT,
    date DATE NOT NULL,
    time TIME NOT NULL,
    duration VARCHAR(50),
    price DECIMAL(10, 2) NOT NULL,
    capacity INTEGER NOT NULL,
    booked INTEGER DEFAULT 0,
    instructor VARCHAR(255),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE bookings (
    id SERIAL PRIMARY KEY,
    class_id INTEGER REFERENCES classes(id),
    customer_name VARCHAR(255) NOT NULL,
    customer_email VARCHAR(255) NOT NULL,
    customer_phone VARCHAR(50),
    participants INTEGER NOT NULL,
    total_amount DECIMAL(10, 2) NOT NULL,
    payment_intent_id VARCHAR(255) UNIQUE,
    status VARCHAR(50) DEFAULT 'confirmed',
    booking_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    cancelled_at TIMESTAMP,
    refund_id VARCHAR(255)
);
```

## 📧 Email Интеграция

Для отправки email подтверждений используйте один из сервисов:

### SendGrid

```bash
npm install @sendgrid/mail
```

```javascript
const sgMail = require('@sendgrid/mail');
sgMail.setApiKey(process.env.SENDGRID_API_KEY);

const msg = {
    to: booking.customerEmail,
    from: 'info@cookingschool.com',
    subject: 'Подтверждение бронирования',
    html: '<strong>Спасибо за бронирование!</strong>'
};

await sgMail.send(msg);
```

### Mailgun

```bash
npm install mailgun-js
```

### AWS SES

```bash
npm install aws-sdk
```

## 🔒 Безопасность

### Рекомендации для Production:

1. **Используйте HTTPS** - обязательно для работы с платежами
2. **Не коммитьте ключи** - добавьте `.env` в `.gitignore`
3. **Валидация данных** - проверяйте все входные данные
4. **Rate limiting** - ограничьте количество запросов
5. **CORS настройка** - разрешайте только доверенные домены
6. **Webhook подпись** - всегда проверяйте подпись Stripe

## 🚀 Deployment

### Heroku

```bash
heroku create your-app-name
git push heroku main
heroku config:set STRIPE_SECRET_KEY=sk_live_...
heroku config:set STRIPE_PUBLISHABLE_KEY=pk_live_...
```

### DigitalOcean / AWS / Google Cloud

1. Установите PM2 для управления процессом:
```bash
npm install -g pm2
pm2 start server.js
pm2 startup
pm2 save
```

2. Настройте Nginx как reverse proxy:
```nginx
server {
    listen 80;
    server_name yourdomain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

## 📝 Customization

### Добавление новых занятий

Отредактируйте массив `classes` в `server.js`:

```javascript
{
    id: 7,
    title: "Новое занятие",
    description: "Описание...",
    date: "2025-12-01",
    time: "18:00",
    duration: "3 часа",
    price: 50,
    capacity: 10,
    booked: 0,
    instructor: "Имя Преподавателя"
}
```

### Изменение дизайна

Все стили находятся в `<style>` блоке `index.html`. Основные цвета:
- Основной: `#667eea`
- Акцент: `#764ba2`

## 🐛 Troubleshooting

### Ошибка "Failed to load classes"
- Убедитесь, что сервер запущен на порту 3000
- Проверьте консоль браузера на ошибки CORS

### Ошибка оплаты
- Проверьте правильность Stripe ключей
- Убедитесь, что используете тестовые карты в тестовом режиме

### Webhook не работает
- В development используйте Stripe CLI для тестирования:
```bash
stripe listen --forward-to localhost:3000/webhook
```

## 📄 Лицензия

MIT License - можете свободно использовать для коммерческих проектов.

## 🤝 Поддержка

Если возникли вопросы:
1. Проверьте документацию Stripe: https://stripe.com/docs
2. Откройте issue в репозитории
3. Напишите на support@example.com

## 🎓 Дальнейшее развитие

Идеи для улучшения:
- [ ] Добавить систему скидок и промокодов
- [ ] Интеграция с календарём (Google Calendar)
- [ ] Отзывы и рейтинги занятий
- [ ] Система лояльности
- [ ] Мобильное приложение
- [ ] Админ панель для управления
- [ ] Интеграция с CRM
- [ ] Повторяющиеся занятия (расписание)
- [ ] Список ожидания для заполненных занятий
- [ ] Групповые скидки

---

Сделано с ❤️ для кулинарных энтузиастов
